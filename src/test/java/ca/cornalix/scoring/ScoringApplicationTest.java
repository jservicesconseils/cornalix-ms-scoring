package ca.cornalix.scoring;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Verifie que le contexte Spring demarre correctement (config, securite,
 * client HTTP) -- rien de plus tant qu'il n'y a pas encore de fonctionnalite
 * additionnelle.
 */
@SpringBootTest
class ScoringApplicationTest {

    @Test
    void contextLoads() {
    }
}
